# whatsacc — working todo

## direction (see ARCHITECTURE.md)
- Go single-binary gateway (SQLite, embedded portal, channel seam) — port from backend/
- controller agent implementing proto/ (pairing, signed commands, offline grants, events)
- Svelte 5 + Tauri app: admin console + LAN/BLE offline emergency access
- Discord channel (WhatsApp + Slack shipped)
- vulos-relay tunnel driver (proto/tunnel.md)

## carried over (still relevant)
- [x] buy domain on namecheap, whatsacc.com
- [x] resend setup, connect dns settings
- [x] firebase hosting (prod + dev sites)
- [x] google oauth set up
- [x] light / dark mode with toggle in top bar
- [x] docs home page with search
- email templates, logo is in public folder
- test google login: after 30 mins are you still logged in? does refresh rotation work?
- basic API system: locations generate API keys; third-party device API with pairing +
  signed keys (proto/ is the spec); pen tests; docs with python/go/curl examples
- test the whatsapp flow end-to-end at a real gate: multi-access-point picker,
  location-first flow, "hi" fallback lists linked locations or says not linked
- avatar system: bunny CDN cache for avatar urls; google avatar url updates; all user
  types can change avatar (or external url); same avatar system for locations
- hide password config for google-only accounts (no password set)
- more docs + syntax highlighting

## removed by decision (do not resurrect)
- billing / paystack / wallet / subscriptions / invoices / VAT / referrals / payouts /
  pricing tiers / FX display — whatsacc has NO billing system. If flagship edge costs
  ever need funding: DMTAP-style postage (see ARCHITECTURE.md), not subscriptions.
